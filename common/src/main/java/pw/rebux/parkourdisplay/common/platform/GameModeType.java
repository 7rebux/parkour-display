package pw.rebux.parkourdisplay.common.platform;

/// Platform-neutral game mode, mapped from LabyMod's `GameMode` / Fabric's `GameMode`.
public enum GameModeType {
  SURVIVAL,
  CREATIVE,
  ADVENTURE,
  SPECTATOR
}
